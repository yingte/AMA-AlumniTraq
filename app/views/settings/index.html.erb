<h1>Settings</h1>

<%= image_tag(Current.user.alumnus.image_as_thumbnail) if (Current.user.role.id == 3 && Current.user.alumnus.image.attached?) %>

<ul>
    <li><%= link_to 'Details', Current.user %></li>
    <% if Current.user.role.id == 3 %>
        <li><%= link_to 'Alumnus Info', Current.user.alumnus %></li>
        <li><%= link_to 'Social Media Handles', new_media_handle_path %></li>
    <% end %>
    <li>3rd Party Authentication</li>
</ul>