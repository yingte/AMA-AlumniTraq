<%= form_with(model: role_permission) do |form| %>
  <% if role_permission.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(role_permission.errors.count, "error") %> prohibited this role_permission from being saved:</h2>

      <ul>
        <% role_permission.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :role_id %>
    <%= form.number_field :role_id %>
  </div>

  <div class="field">
    <%= form.label :permission_id %>
    <%= form.number_field :permission_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
