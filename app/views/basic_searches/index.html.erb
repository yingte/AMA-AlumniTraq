<div class="container">
    <h1><%= t("directory") %></h1>

    <% if flash.any? %>
        <% flash.each do |key, value| -%>
        <section class="flash flash__<%= key %>"><%= value.html_safe %></section>
        <% end -%>
    <% end %>

    <%= render 'form' %>
</div>
